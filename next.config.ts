/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "source.unsplash.com",
      "picsum.photos",
      "i.pravatar.cc",
      "placehold.co",
    ],
  },
};

export default nextConfig;
